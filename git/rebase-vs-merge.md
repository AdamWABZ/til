# Rebase vs merge

- `git merge` keeps the full history and adds a merge commit.
- `git rebase` rewrites history to make it linear and cleaner.

Use merge for shared branches, rebase for local cleanup before a PR.
